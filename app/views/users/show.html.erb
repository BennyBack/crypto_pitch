  <div class="media">
    <%= image_tag current_user.profile_pic.url(:thumb), class: "pb-4" %>
    <div class="media-body">
      <h5 class="mt-0">Name: <%= "#{current_user.first_name + current_user.last_name}" %></h5>
      <h3>Phone number:<%= current_user.phone_number %></h3>
      <h4>Email: <%= current_user.email %></h4>
    </div>
  </div>
 
